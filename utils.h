#ifndef UTILS_H
#define UTILS_H

#include <SDL.h>
#include "./stb_image.h"
#include <iostream>
#include <fstream>

namespace util {
	static const int WINDOW_X = SDL_WINDOWPOS_CENTERED;
	static const int WINDOW_Y = SDL_WINDOWPOS_CENTERED;
	static const int WINDOW_W = 800;
	static const int WINDOW_H = 800;
}

#endif